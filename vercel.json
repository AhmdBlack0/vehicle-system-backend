{
  "buildCommand": "npm install && npx prisma generate && npx prisma migrate deploy",
  "outputDirectory": "",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
